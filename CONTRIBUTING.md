# 🤝 Руководство по контрибьютингу

Спасибо за интерес к проекту Gropy! Мы приветствуем вклад в развитие приложения.

## 🚀 Быстрый старт

### Установка
```bash
# Клонирование репозитория
git clone https://github.com/LitovPro/gropy.git
cd gropy

# Установка зависимостей
npm install

# Запуск в режиме разработки
npm run dev
```

### Доступные команды
```bash
npm run dev          # Запуск dev-сервера
npm run build        # Сборка для production
npm run preview      # Предварительный просмотр сборки
npm run test         # Запуск тестов
npm run lint         # Проверка кода линтером
npm run lint:fix     # Автоисправление ошибок линтера
npm run format       # Форматирование кода
npm run format:check # Проверка форматирования
```

## 📋 Процесс разработки

### 1. Создание ветки
```bash
git checkout -b feature/amazing-feature
# или
git checkout -b fix/bug-description
```

### 2. Внесение изменений
- Следуйте существующему стилю кода
- Добавляйте комментарии для сложной логики
- Обновляйте документацию при необходимости

### 3. Тестирование
```bash
npm run test         # Запуск тестов
npm run lint         # Проверка кода
npm run format:check # Проверка форматирования
```

### 4. Коммит
```bash
git add .
git commit -m "feat: add amazing feature"
```

### 5. Push и Pull Request
```bash
git push origin feature/amazing-feature
```

## 📝 Соглашения о коммитах

Используем [Conventional Commits](https://www.conventionalcommits.org/):

```
<type>[optional scope]: <description>

[optional body]

[optional footer(s)]
```

### Типы коммитов
- `feat`: новая функциональность
- `fix`: исправление бага
- `docs`: изменения в документации
- `style`: форматирование, отсутствующие точки с запятой и т.д.
- `refactor`: рефакторинг кода
- `test`: добавление тестов
- `chore`: обновление задач сборки, конфигурации и т.д.

### Примеры
```bash
git commit -m "feat(rituals): add new breathing exercise"
git commit -m "fix(diary): resolve mood tracking bug"
git commit -m "docs: update README with new features"
git commit -m "style: format code with prettier"
```

## 🎨 Стиль кода

### TypeScript
- Используйте строгую типизацию
- Избегайте `any`
- Используйте интерфейсы для объектов
- Добавляйте JSDoc для сложных функций

### React
- Используйте функциональные компоненты
- Применяйте хуки вместо классовых компонентов
- Используйте `useCallback` и `useMemo` для оптимизации
- Следуйте принципам чистой архитектуры

### Styled Components
- Используйте темизацию через `ThemeProvider`
- Создавайте переиспользуемые компоненты
- Применяйте семантические имена

### Примеры

#### ✅ Хорошо
```typescript
interface RitualProps {
  id: string;
  title: string;
  onComplete: (id: string) => void;
}

const RitualCard: React.FC<RitualProps> = ({ id, title, onComplete }) => {
  const handleClick = useCallback(() => {
    onComplete(id);
  }, [id, onComplete]);

  return (
    <CardContainer onClick={handleClick}>
      <Title>{title}</Title>
    </CardContainer>
  );
};
```

#### ❌ Плохо
```typescript
const RitualCard = (props: any) => {
  return (
    <div onClick={() => props.onComplete(props.id)}>
      <h1>{props.title}</h1>
    </div>
  );
};
```

## 🧪 Тестирование

### Структура тестов
```
src/tests/
├── components/     # Тесты компонентов
├── hooks/         # Тесты хуков
├── utils/         # Тесты утилит
├── setup.ts       # Настройка тестов
└── test-utils.tsx # Утилиты для тестов
```

### Написание тестов
```typescript
import { render, screen } from '@testing-library/react';
import { RitualCard } from '../RitualCard';

describe('RitualCard', () => {
  it('renders ritual title', () => {
    render(<RitualCard id="test" title="Test Ritual" onComplete={jest.fn()} />);
    expect(screen.getByText('Test Ritual')).toBeInTheDocument();
  });
});
```

## 🔒 Безопасность

### Принципы
- Все пользовательские данные санитизируются с помощью DOMPurify
- Никаких внешних трекеров или аналитики
- Локальное хранение данных
- Регулярные аудиты зависимостей

### Проверка безопасности
```bash
npm audit              # Проверка уязвимостей
npm audit fix          # Автоисправление
npm run lint           # Проверка кода
```

## 📚 Документация

### Обновление документации
- Обновляйте README.md при добавлении новых функций
- Добавляйте JSDoc комментарии к функциям
- Обновляйте типы в `src/types/`
- Документируйте новые хуки и утилиты

### Структура документации
```
├── README.md          # Основная документация
├── SECURITY.md        # Политика безопасности
├── CONTRIBUTING.md    # Это руководство
└── DEPLOYMENT.md      # Инструкции по деплою
```

## 🐛 Сообщение о багах

### Шаблон Issue
```markdown
**Описание бага**
Краткое описание проблемы.

**Шаги для воспроизведения**
1. Перейти к '...'
2. Нажать на '...'
3. Увидеть ошибку

**Ожидаемое поведение**
Что должно было произойти.

**Скриншоты**
Если применимо, добавьте скриншоты.

**Окружение:**
- ОС: [например, Windows 10]
- Браузер: [например, Chrome 91]
- Версия: [например, 1.0.0]
```

## 💡 Предложения функций

### Шаблон Feature Request
```markdown
**Описание функции**
Четкое описание желаемой функции.

**Проблема, которую это решает**
Какую проблему решает эта функция?

**Предлагаемое решение**
Как вы видите реализацию?

**Альтернативы**
Какие альтернативные решения вы рассматривали?
```

## 📞 Получение помощи

- **GitHub Issues**: [Создать Issue](https://github.com/LitovPro/gropy/issues/new)
- **Discussions**: [Обсуждения](https://github.com/LitovPro/gropy/discussions)
- **Email**: [security@gropy.app](mailto:security@gropy.app) (только для безопасности)

## 📄 Лицензия

Участвуя в проекте, вы соглашаетесь с тем, что ваш вклад будет лицензирован под MIT License.

---

**Спасибо за ваш вклад в Gropy! 💚**
